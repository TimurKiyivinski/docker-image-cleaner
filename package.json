{
  "name": "docker-image-cleaner",
  "engines" : { "node" : ">=7.0.0" },
  "version": "1.0.0",
  "description": "Remove old docker image builds by a count treshold",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "docker"
  ],
  "author": "Timur Kiyivinski",
  "license": "MIT",
  "dependencies": {
    "dockerode": "^2.3.1",
    "node-schedule": "^1.2.0"
  }
}
